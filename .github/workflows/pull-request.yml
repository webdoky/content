on:
  push:
    branches:
      - "translation/*"
      - "update/*"

jobs:
  pull-request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: pull-request
        uses: diillson/auto-pull-request@v1.0.1
